{
  "version": 2,
  "buildCommand": "node -e \"const fs=require('fs'), {execSync}=require('child_process'); const root = (!fs.existsSync('backend') && fs.existsSync('../backend')) ? '..' : '.'; execSync('npm --prefix ' + root + '/backend install && npm --prefix ' + root + '/frontend install && npm --prefix ' + root + '/frontend run build', {stdio: 'inherit'});\"",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/health",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
