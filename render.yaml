services:
  - type: web
    name: vigil-app
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node backend/server.js
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
